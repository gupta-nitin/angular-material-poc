  <tr mat-row *matRowDef="let row; columns: displayedColumns;"
        class="element-row" [class.expanded-element-row]="expandedElement === row"
        (click)="expandedElement = expandedElement === row ? null : row"></tr>
    <tr mat-row *matRowDef="let row; columns: ['expandedDetail']" class="detail-row"></tr>

    <!-- Expanded Content Column - Detail Row -->
    <ng-container matColumnDef="expandedDetail">
      <td mat-cell *matCellDef="let element" [attr.colspan]="displayedColumns.length">
        <div class="element-detail" *ngIf="expandedElement === element">
          <table mat-table [dataSource]="element.marks" class="inner-table">
            <ng-container matColumnDef="subject">
              <th mat-header-cell *matHeaderCellDef> Subject </th>
              <td mat-cell *matCellDef="let mark"> {{mark.subject}} </td>
            </ng-container>
            <ng-container matColumnDef="mark">
              <th mat-header-cell *matHeaderCellDef> Mark </th>
              <td mat-cell *matCellDef="let mark"> {{mark.mark}} </td>
            </ng-container>
            <tr mat-header-row *matHeaderRowDef="['subject', 'mark']"></tr>
            <tr mat-row *matRowDef="let row; columns: ['subject', 'mark']"></tr>
          </table>
        </div>
      </td>
    </ng-container>

------------------

/* Styles for the main table */
table {
  width: 100%;
}

.mat-form-field {
  width: 100%;
}

/* Styles for the inner table */
.inner-table {
  margin-top: 10px;
  margin-bottom: 10px;
}

.element-row {
  cursor: pointer;
}

.detail-row {
  height: 0;
}

.element-detail {
  padding: 16px;
  box-sizing: border-box;
  overflow: hidden;
  display: flex;
  justify-content: center;
}
