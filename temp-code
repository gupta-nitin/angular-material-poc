/* dashboard.component.css */
.header-cell {
  position: relative;
}

.options {
  display: none;
  position: absolute;
  top: 50%;
  right: 0;
  transform: translateY(-50%);
}

.header-cell:hover .options {
  display: block;
}

<table mat-table [dataSource]="dataSource" matSort class="mat-elevation-z8">
    <!-- Contact Name Column -->
    <ng-container matColumnDef="contactName">
      <th mat-header-cell *matHeaderCellDef mat-sort-header (click)="showOptions('contactName')" class="header-cell">
        <div class="header-content">
          <span>Contact Name</span>
          <span class="options" *ngIf="showOptionsFor === 'contactName'"> <!-- Show options only when clicked -->
            <mat-icon (click)="sortColumn('contactName')">sort</mat-icon>
            <mat-icon (click)="autoSizeColumn('contactName')">aspect_ratio</mat-icon>
            <mat-icon (click)="resetColumn('contactName')">settings_backup_restore</mat-icon>
          </span>
        </div>
      </th>
      <td mat-cell *matCellDef="let row">{{ row.contactName }}</td>
    </ng-container>
