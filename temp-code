 onReset(event: Event): void {
    event.stopPropagation();
    this.filterForm.patchValue({
      contractName: '',
      processingDate: ''
    });
    this.filters = [];
    this.searchCriteria.emit(this.filters);
  }
